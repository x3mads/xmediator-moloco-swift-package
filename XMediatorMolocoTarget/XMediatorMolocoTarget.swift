// Intentionally empty.
// This wrapper target exists so the XMediatorMoloco product can combine the
// XMediatorMoloco binary target and carry their required
// system-framework link settings (binary targets cannot declare either).
